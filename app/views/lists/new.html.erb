<div class="container my-5">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <h1 class="mb-4">Create a new list</h1>

        <%= simple_form_for @list do |f| %>
        <%= f.input :name, placeholder: "e.g. Drama, Classics...", required: false %>
        <%= f.input :image_url, label: "Cover image URL", placeholder: "https://..." %>
        <%= f.submit "Create List", class: "btn btn-success" %>
      <% end %>

      <%= link_to "← Back to lists", lists_path, class: "btn btn-link mt-2" %>
    </div>
  </div>
</div>
